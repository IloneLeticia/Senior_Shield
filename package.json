{
  "name": "senior-shield-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx json-server --host 0.0.0.0 --watch src/dados.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}