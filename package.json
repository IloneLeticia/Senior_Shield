{
  "name": "senior-shield-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --host 0.0.0.0 --watch src/dados.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
